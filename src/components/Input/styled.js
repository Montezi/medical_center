import styled from 'styled-components';
import { Input as InputRoot } from '@ui-kitten/components';

export const Input = styled(InputRoot)`
  width: 80%;
  margin-top: 10px;
  background-color: #FFFFFF;
  border-color: #FFFFFF;
`;
